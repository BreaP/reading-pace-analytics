# Reading Progress & Pace Analytics Tool

A custom Python-based command-line utility built to dynamically track reading history, evaluate pacing velocity against explicit yearly targets, and deliver automated data-driven progress alerts.

## 📊 Business Logic & Data Metrics
While designed around a personal reading hobby, this project implements core data analysis workflows:
* **Target Benchmarking:** Calculates data velocity requirements dynamically based on remaining timeframes.
* **Data Ingestion:** Automates data structuring using a structured schema (Python dictionaries inside a nested collection).
* **Automated Data Alerts:** Features conditional logic to identify performance deficits relative to seasonal benchmarks.

## 🛠️ Tech Stack & Concepts Used
* **Language:** Python 3
* **Data Structures:** Lists, Dictionaries (Key-Value Pair mapping)
* **Logic:** Conditional branching (`if/else`), custom function engineering, string formatting.

## 🚀 How It Works
The script evaluates the total goal against the current chronological milestone of the year, rendering real-time pacing adjustments:
* **Reading Progress Dashboard:** Dynamically counts logged books and factors in historical baselines to calculate required weekly reading targets.
* **Pace Verification Engine:** Evaluates current progress ratios against dynamic calendar benchmarks to trigger contextual "ahead of pace" or "behind pace" status alerts.


## 🕹️ Project 2: Sims-Inspired Text Adventure Engine
A branching decision-tree simulator designed to model user behavior pathways, state variable changes, and dynamic event generation.

### Core Data Analyst Workflows Applied:
* **State Management:** Tracks shifting quantitative variables (`simoleons`, `energy`, `fun`) across runtime operations.
* **Stochastic Simulation:** Utilizes the Python `random` library to simulate unpredictable, real-world data variables.
* **Iterative Logging:** Captures historical user interaction choices and parses them into a chronological text-analytics summary report using `for` loops.
